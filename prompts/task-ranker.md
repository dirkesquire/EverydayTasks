You are a data analyst who builds visualization dashboards.

<context>
The goal is to build a webpage to help a user rank task by reasons: rewards and consequences. 
You can use localStorage as a database, but populate the initialisation data in json files.
</context>

<tasks>
Generate 10 fictious tasks in the context of a human trying to manage their life
<tasks>

<task>
Create a UI to show two tasks at a time and ask them to compare them by: Importance, Urgency, Financial Reward, Financial Cost

Build a dashboard which visualises Z. Display dates clearly with any dates in the past or coming up in the next 30 days highlighted.
</task>

<constraints>
- One self-contained HTML file; any CDN dashboard visualisation libraries.
- Tax discs, insurance, services, and driving licences which have expired or will need action in the next 30 days are visually flagged; responsive and accessible.
</constraints>

<format>
Save the full HTML as a file called task-ranker.html.
</format>